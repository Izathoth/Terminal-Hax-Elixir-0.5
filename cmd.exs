{
  "lst": {
    "comando": "lst |> {diretorio}",
    "descricao": "Lista todos os arquivos de um diretório."
  },
  "cpy": {
    "comando": "cpy > {arquivo_origem} < {destino}",
    "descricao": "Copia um arquivo de um local para outro."
  },
  "del": {
    "comando": "del | {arquivo}",
    "descricao": "Deleta o arquivo especificado."
  },
  "exe": {
    "comando": "exe $ {comando}",
    "descricao": "Executa um comando no terminal."
  },
  "ren": {
    "comando": "ren > {arquivo_origem} < {novo_nome}",
    "descricao": "Renomeia um arquivo."
  },
  "mov": {
    "comando": "mov > {arquivo_origem} < {destino}",
    "descricao": "Move um arquivo de um local para outro."
  },
  "mkdir": {
    "comando": "mkdir |> {diretorio}",
    "descricao": "Cria um novo diretório."
  },
  "cat": {
    "comando": "cat > {arquivo_saida} < {arquivo_entrada1} {arquivo_entrada2}",
    "descricao": "Concatena o conteúdo de vários arquivos em um arquivo de saída."
  },
  "schd": {
    "comando": "schd $ {intervalo} {comando}",
    "descricao": "Agenda um comando para ser executado em intervalos regulares de tempo (em segundos)."
  }
}